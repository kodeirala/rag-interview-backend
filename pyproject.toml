[project]
name = "rag-interview-backend"
version = "1.0.0"
description = "FastAPI backend for document ingestion, conversational RAG, and interview booking"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
